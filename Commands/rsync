 rsync -a --include '*/' --exclude '*' /path/to/source/* /path/to/dest # copy directory

rsync -avz -e "ssh -p $portNumber" user@remoteip:/path/to/files/ /local/path/

#!/usr/bin/env bash

rsync -avuhP -e "ssh -p 10822" * zhouxiangyong@s108:~/Proj/coronary

