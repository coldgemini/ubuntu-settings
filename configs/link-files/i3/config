gaps inner 6
gaps outer 0
#xprop click on window to get Class
for_window [class="^.*"] border pixel 0
new_window 1pixel
# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 16

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
# bindsym $mod+Return exec urxvt
bindsym $mod+Return exec gnome-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Workspace names
# to display names or symbols instead of plain workspace numbers you can use
# something like: set $ws1 1:mail
#                 set $ws2 2:
#set $ws1 1
#set $ws2 2
#set $ws3 3
#set $ws4 4
#set $ws5 5
#set $ws6 6
#set $ws7 7
#set $ws8 8

# switch to workspace
bindsym $mod+1 workspace 1:1CMD
bindsym $mod+2 workspace 2:2CMD
bindsym $mod+3 workspace 3:3CMD
bindsym $mod+4 workspace 4:4CMD
bindsym $mod+5 workspace 5:5CMD
bindsym $mod+6 workspace 6:6CMD
bindsym $mod+7 workspace 7:7CMD
bindsym $mod+8 workspace 8:8CMD
bindsym $mod+9 workspace 9:9CMD
bindsym $mod+0 workspace 0:0CMD
bindsym $mod+Ctrl+1 workspace 1:C1CMD
bindsym $mod+Ctrl+2 workspace 2:C2CMD
bindsym $mod+Ctrl+3 workspace 3:C3CMD
bindsym $mod+Ctrl+4 workspace 4:C4CMD
bindsym $mod+Ctrl+5 workspace 5:C5CMD
bindsym $mod+Ctrl+6 workspace 6:C6CMD
bindsym $mod+Ctrl+7 workspace 7:C7CMD
bindsym $mod+Ctrl+8 workspace 8:C8CMD
bindsym $mod+Ctrl+9 workspace 9:C9CMD
bindsym $mod+Ctrl+0 workspace 0:C0CMD
bindsym $mod+Shift+1 workspace 1:S1CMD
bindsym $mod+Shift+2 workspace 2:S2CMD
bindsym $mod+Shift+3 workspace 3:S3CMD
bindsym $mod+Shift+4 workspace 4:S4CMD
bindsym $mod+Shift+5 workspace 5:S5CMD
bindsym $mod+Shift+6 workspace 6:S6CMD
bindsym $mod+Shift+7 workspace 7:S7CMD
bindsym $mod+Shift+8 workspace 8:S8CMD
bindsym $mod+Shift+9 workspace 9:S9CMD
bindsym $mod+Shift+0 workspace 0:S0CMD
bindsym $mod+Ctrl+t workspace TMP
bindsym $mod+Ctrl+x workspace TMPX
bindsym $mod+Ctrl+k workspace TASK
bindsym $mod+Ctrl+z workspace ZEAL
bindsym $mod+Ctrl+d workspace DOCS
bindsym $mod+Ctrl+w workspace WEB
bindsym $mod+Ctrl+g workspace GIMP
bindsym $mod+Ctrl+n workspace NOTE
bindsym $mod+Ctrl+a workspace AUDIO
bindsym $mod+Ctrl+v workspace VIDEO
bindsym $mod+Ctrl+b workspace VBOX
bindsym $mod+Ctrl+Shift+w move container to workspace WEB
bindsym $mod+Ctrl+Shift+1 workspace CODE1
bindsym $mod+Ctrl+Shift+2 workspace CODE2
bindsym $mod+Ctrl+Shift+3 workspace CODE3
bindsym $mod+Ctrl+Shift+4 workspace CODE4
bindsym $mod+Ctrl+Shift+5 workspace CODE5
bindsym $mod+Ctrl+Shift+6 workspace CODE6
bindsym $mod+Ctrl+Shift+7 workspace CODE7
bindsym $mod+Ctrl+Shift+8 workspace CODE8
bindsym $mod+Ctrl+Shift+9 workspace CODE9
bindsym $mod+Ctrl+Shift+0 workspace CODE0

# move focused container to workspace
#bindsym $mod+Shift+1 move container to workspace 1:1CMD
#bindsym $mod+Shift+2 move container to workspace 2:2CMD
#bindsym $mod+Shift+3 move container to workspace 3:3CMD
#bindsym $mod+Shift+4 move container to workspace 4:4CMD
#bindsym $mod+Shift+5 move container to workspace 5:5CODE
#bindsym $mod+Shift+6 move container to workspace 6:6CODE
#bindsym $mod+Shift+7 move container to workspace 7:7BWR
#bindsym $mod+Shift+8 move container to workspace 8:8MED
#bindsym $mod+Shift+9 move container to workspace 9:9NOTE
#bindsym $mod+Shift+0 move container to workspace 10:0RSV

#open in floating mode
for_window [class="Eog"] floating enable
for_window [class="Mask"] floating enable
for_window [class="^i3f*"] floating enable

# Open applications on specific workspaces
#assign [class="URxvt"] $ws1
assign [class="google-chrome"] WEB
assign [class="Firefox"] WEB
assign [class="netease-cloud-music"] workspace AUDIO
#assign [class="/usr/bin/code"] workspace 3:3CODE
# assign [class="Skype"] $ws5

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Shift+e exec "i3-msg exit"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 5 px or 5 ppt
        bindsym k resize grow height 5 px or 5 ppt
        bindsym l resize shrink height 5 px or 5 ppt
        bindsym semicolon resize grow width 5 px or 5 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 2 px or 2 ppt
        bindsym Down resize grow height 2 px or 2 ppt
        bindsym Up resize shrink height 2 px or 2 ppt
        bindsym Right resize grow width 2 px or 2 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bindsym $mod+m bar mode toggle
bar {
        status_command i3status
        tray_output primary
		font xft:Noto Sans 12
		strip_workspace_numbers yes

		colors {
#       background $transparent
			background #2B2C2B
			statusline #F9FAF9
			separator  #454947

#                              border  backgr. text
			focused_workspace  #F9FAF9 #16A085 #2B2C2B
			active_workspace   #595B5B #353836 #FDF6E3
			inactive_workspace #595B5B #353836 #EEE8D5
			urgent_workspace   #16A085 #FDF6E3 #E5201D
		}
}

#user setting
exec --no-startup-id nm-applet
#exec --no-startup-id feh --bg-scale /usr/share/backgrounds/Cielo_estrellado_by_Eduardo_Diez_Viñuela.jpg
exec --no-startup-id feh --bg-scale $HOME/Pictures/background.jpg
bindsym $mod+Shift+w exec /usr/bin/google-chrome
bindsym $mod+Shift+f exec /usr/bin/firefox
bindsym $mod+Shift+m exec /usr/bin/code -n
bindsym $mod+Shift+u exec /usr/bin/netease-cloud-music
bindsym $mod+Shift+n exec /home/xiang/apps/boostnote/usr/share/boostnote/Boostnote
bindsym $mod+Shift+z exec /usr/bin/zeal
#bindsym $mod+Shift+g exec /usr/bin/gimp
bindsym $mod+Shift+g exec flatpak run org.gimp.GIMP
bindsym $mod+Tab workspace back_and_forth

# bindsym $mod+x exec i3lock
# bindsym $mod+p exec /home/xiang/.i3/i3lock-fancy-multimonitor/lock
bindsym $mod+p exec /home/xiang/Src/i3lock-fancy/lock
# bindsym $mod+F12	[instance="gnome-terminal" title="Life"] scratchpad show
# bindsym $mod+q scratchpad show
# exec --no-startup-id urxvt -T "Life"
# for_window [instance="urxvt" title="Life"] floating enable, move scratchpad, scratchpad show;
# for_window [instance="urxvt" title="Life"] resize grow left 300, resize grow right 300, resize grow down 150, resize grow up 100, move scratchpad;
# bindsym Menu  [instance="urxvt" title="Life"] scratchpad show
# bindsym Print [instance="urxvt" title="Life"] scratchpad show
# exec --no-startup-id gnome-terminal -e /bin/bash -l KJ -T "Life"
# exec --no-startup-id gnome-terminal -e /bin/bash -T "Life"
exec --no-startup-id gnome-terminal -e /bin/bash
# exec urxvt -e /bin/bash
# for_window [instance="Gnome-terminal" title="Life"] floating enable, move scratchpad, scratchpad show;
# for_window [title="Life"] floating enable, move scratchpad, scratchpad show;
# for_window [Class="Gnome-terminal" Title="Life"] floating enable, move scratchpad, scratchpad show;
# for_window [instance="gnome-terminal-server" title="Life"] floating enable, move scratchpad, scratchpad show;
# for_window [instance="gnome-terminal-server"] floating enable, move scratchpad, scratchpad show;
bindsym $mod+Shift+BackSpace move scratchpad
bindsym $mod+backslash scratchpad show
